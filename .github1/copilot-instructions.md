# Copilot Instructions

> This file is generated by the scaffolding phase. All specifics below should be derived from the app spec document and `docs/requirements.md`.

## Project Overview

Describe the application purpose, architecture style, and high-level structure here. This section should be populated during scaffolding based on the app spec.

## Architecture

- **Structure**: Define the top-level folder layout (e.g., monorepo, single-app, etc.)
- **Frontend**: Framework, language, styling, routing, state management
- **Backend**: Framework, language, validation, authentication
- **Shared**: Common types, utilities, or contracts consumed across layers

## Coding Standards

### TypeScript
- Strict mode enabled everywhere (`"strict": true`)
- No `any` types — use proper interfaces
- Prefer `const` over `let`; never use `var`

### Backend Conventions
- Routes should be thin handlers that delegate to business logic
- Middleware for cross-cutting concerns: auth, validation, error handling
- All request bodies validated with a schema validation library before processing
- Consistent error response format: `{ error: { code, message, details? } }`

### Frontend Conventions
- Pages organized by route
- Reusable components separated from page-level components
- API calls through a centralized fetch/client wrapper
- Tailwind CSS for all styling — no inline styles, no CSS modules
- Forms use a form management library (e.g., React Hook Form)

### Shared Types
- All API request/response shapes defined in a shared types location
- Import using path aliases where possible

## Key Business Rules

> Populate from the app spec. List domain-specific rules, lifecycle constraints, access control policies, and validation requirements.

## Testing
- Define the test runner and strategy (unit, integration, e2e)
- Backend: API integration tests
- Frontend: Component tests

## Scripts
- `npm run dev` — start the development environment
- `npm run install:all` — install all workspace dependencies
- `npm test` — run all tests

## Key Files
| File | Purpose |
|------|---------|
| `<app-spec>.md` | Source requirements |
| `docs/requirements.md` | Tracked requirements with status |
| `docs/architecture.md` | System architecture |
| `docs/tech-stack.md` | Technology choices & rationale |
